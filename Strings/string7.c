//  concatenation using strcat()
#include <stdio.h>
#include <string.h>
#define max_size 100

int main()
{
    char source[max_size], target[max_size];

    printf("Enter Your full name : ");
    gets(source);

    return 0;
}